<section class="py-20 px-6 bg-gray-50">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-4xl md:text-5xl font-extrabold text-gray-900 mb-4 tracking-tight">
        Contáctanos
      </h2>
      <p class="text-lg text-gray-600 mb-12">
        ¿Listo para vivir la experiencia? Escríbenos y te responderemos lo antes posible.
      </p>
    </div>
  
    <div class="max-w-xl mx-auto bg-white shadow-xl rounded-2xl p-8 space-y-6">
      <form class="space-y-4">
        <input
          type="text"
          placeholder="Nombre completo"
          class="w-full border border-gray-300 rounded-xl px-4 py-3 text-gray-700 focus:ring-2 focus:ring-blue-500 focus:outline-none transition"
        />
        <input
          type="email"
          placeholder="Correo electrónico"
          class="w-full border border-gray-300 rounded-xl px-4 py-3 text-gray-700 focus:ring-2 focus:ring-blue-500 focus:outline-none transition"
        />
        <textarea
          rows="4"
          placeholder="Tu mensaje..."
          class="w-full border border-gray-300 rounded-xl px-4 py-3 text-gray-700 focus:ring-2 focus:ring-blue-500 focus:outline-none transition"
        ></textarea>
        <button
          type="submit"
          class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-xl shadow-md hover:shadow-lg transition transform hover:scale-[1.02]"
        >
          📩 Enviar Mensaje
        </button>
      </form>
  
      <div class="text-center text-gray-600">
        <p>
          También puedes escribirnos por
          <a
            href="https://wa.me/51999999999"
            class="text-blue-600 font-medium underline hover:text-blue-700"
            target="_blank"
            rel="noopener noreferrer"
          >
            WhatsApp
          </a>.
        </p>
      </div>
    </div>
  </section>
  